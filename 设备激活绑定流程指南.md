# TuyaOpen 设备激活绑定流程全指南

## 目录
1. [概述](#概述)
2. [核心数据结构](#核心数据结构)
3. [HTTP激活流程](#http激活流程)
4. [网络配网流程](#网络配网流程)
5. [完整API参考](#完整api参考)
6. [示例代码实现](#示例代码实现)
7. [最佳实践](#最佳实践)

## 概述

TuyaOpen设备激活绑定是设备接入涂鸦云平台的核心流程，包含以下主要步骤：

1. **设备初始化** - 配置设备参数和证书
2. **网络配网** - 通过AP、BLE等方式获取WiFi配置和Token
3. **HTTP激活** - 通过ATOP协议向云端发送激活请求  
4. **MQTT连接** - 建立长连接进行数据通信
5. **状态同步** - 设备状态上报和云端数据同步

## 核心数据结构

### 1. IoT客户端配置

```c
typedef struct {
    const char *productkey;      // 产品KEY，在涂鸦平台创建产品时获得
    const char *uuid;            // 设备唯一标识符
    const char *pincode;         // PIN码，用于安全验证
    const char *authkey;         // 认证密钥，设备激活凭证
    const char *software_ver;    // 软件版本号
    const char *modules;         // 可选：设备模组信息
    const char *skill_param;     // 可选：技能参数
    const char *storage_namespace; // 可选：存储命名空间
    const char *firmware_key;    // 可选：固件密钥
    event_handle_cb_t event_handler;    // 事件处理回调函数
    network_check_cb_t network_check;   // 网络检查回调函数
    tuya_ota_event_cb_t ota_handler;    // OTA升级回调函数
} tuya_iot_config_t;
```

### 2. 设备激活数据

```c
typedef struct {
    char devid[MAX_LENGTH_DEVICE_ID + 1];     // 设备ID，激活后云端分配
    char seckey[MAX_LENGTH_SECKEY + 1];       // 安全密钥，用于后续通信加密
    char localkey[MAX_LENGTH_LOCALKEY + 1];   // 本地密钥，用于局域网通信
    char schemaId[MAX_LENGTH_SCHEMA_ID + 1];  // 数据点模型ID
    char timezone[MAX_LENGTH_TIMEZONE + 1];   // 时区信息
    bool resetFactory;                        // 是否恢复出厂设置标志
    int capability;                           // 设备能力标识
} tuya_activated_data_t;
```

### 3. 绑定信息

```c
typedef struct {
    char token[MAX_LENGTH_TOKEN + 1];      // 绑定Token，配网时获得
    char region[MAX_LENGTH_REGION + 1];    // 区域信息（如：cn、us、eu）  
    char regist_key[MAX_LENGTH_REGIST + 1]; // 注册环境（如：pro、test）
} tuya_binding_info_t;
```

### 4. ATOP请求结构

```c
typedef struct {
    const char *path;        // 请求路径，通常为"/d.json"
    const char *key;         // 认证密钥或设备密钥
    const char *api;         // API接口名称
    const char *version;     // API版本号
    const char *uuid;        // 设备UUID（激活前使用）
    const char *devid;       // 设备ID（激活后使用）
    uint32_t timestamp;      // 时间戳
    void *data;              // 请求数据（JSON字符串）
    size_t datalen;          // 数据长度
    const void *user_data;   // 用户自定义数据
} atop_base_request_t;
```

### 5. ATOP响应结构

```c
typedef struct {
    bool success;            // 请求是否成功
    cJSON *result;          // 响应结果JSON对象
    int32_t t;              // 服务器时间戳
    void *user_data;        // 用户自定义数据
    uint8_t *raw_data;      // 原始响应数据
    size_t raw_data_len;    // 原始数据长度
} atop_base_response_t;
```

## HTTP激活流程

### 1. 激活请求构建

```c
// 激活请求参数结构
typedef struct {
    const char *token;          // 配网获得的Token
    const char *product_key;    // 产品密钥
    const char *firmware_key;   // 固件密钥（可选）
    const char *uuid;           // 设备UUID
    const char *devid;          // 设备ID（重激活时使用）
    const char *authkey;        // 认证密钥
    const char *sw_ver;         // 软件版本
    const char *pv;             // 协议版本
    const char *bv;             // 基线版本
    const char *modules;        // 模组信息（可选）
    const char *feature;        // 特性信息（可选）
    const char *skill_param;    // 技能参数（可选）
    const void *user_data;      // 用户数据
} tuya_activite_request_t;
```

### 2. 激活流程步骤

#### 步骤1：构建激活请求

激活请求的JSON格式：
```json
{
    "token": "激活Token",
    "softVer": "软件版本",
    "productKey": "产品密钥", 
    "protocolVer": "协议版本",
    "baselineVer": "基线版本",
    "options": "{\"otaChannel\":0, \"isFK\":false}",
    "devAttribute": 2048,
    "cadVer": "1.0.3",
    "cdVer": "1.0.0",
    "t": 时间戳
}
```

#### 步骤2：发送HTTP请求

```c
int atop_service_activate_request(const tuya_activite_request_t *request, 
                                  atop_base_response_t *response)
{
    // 1. 构建POST数据
    uint32_t timestamp = tal_time_get_posix();
    
    // 2. 构建ATOP请求
    atop_base_request_t atop_request = {
        .uuid = request->uuid,
        .key = request->authkey,
        .path = "/d.json",
        .timestamp = timestamp,
        .api = "thing.device.opensdk.active",
        .version = "1.0",
        .data = buffer,
        .datalen = offset,
        .user_data = request->user_data
    };
    
    // 3. 发送HTTP请求
    int rt = atop_base_request(&atop_request, response);
    return rt;
}
```

#### 步骤3：解析激活响应

激活成功响应包含：
```json
{
    "success": true,
    "result": {
        "devId": "设备ID",
        "secKey": "安全密钥", 
        "localKey": "本地密钥",
        "schemaId": "数据点模型ID",
        "stdTimeZone": "时区",
        "schema": "数据点模型JSON"
    },
    "t": 时间戳
}
```

## 网络配网流程

### 1. WiFi AP配网

#### AP配网初始化

```c
int ap_netcfg_init(netcfg_args_t *netcfg_args)
{
    // 注册AP配网处理器
    return netcfg_register(NETCFG_TUYA_WIFI_AP, ap_netcfg_start, ap_netcfg_stop);
}
```

#### AP配网启动流程

```c
static int ap_mode_start(ap_netcfg_t *ap)
{
    // 1. 设置WiFi为AP模式
    int op_ret = tal_wifi_set_work_mode(WWM_SOFTAP);
    
    // 2. 配置AP参数
    WF_AP_CFG_IF_S ap_cfg = {0};
    strcpy(ap_cfg.ip.ip, "192.168.176.1");
    strcpy(ap_cfg.ip.gw, "192.168.176.1"); 
    strcpy(ap_cfg.ip.mask, "255.255.255.0");
    
    // 3. 生成AP SSID（基于MAC地址）
    NW_MAC_S mac;
    tal_wifi_get_mac(WF_AP, &mac);
    sprintf((char *)ap_cfg.ssid, "%s-%02X%02X", 
            TUYA_AP_SSID_DEFAULT, mac.mac[4], mac.mac[5]);
    
    // 4. 启动AP
    op_ret = tal_wifi_ap_start(&ap_cfg);
    return op_ret;
}
```

#### 配网数据接收

APP连接设备AP后，发送配网数据：
```json
{
    "ssid": "目标WiFi名称",
    "password": "WiFi密码", 
    "token": "绑定Token"
}
```

### 2. BLE配网

#### BLE配网初始化

```c
int ble_netcfg_init(netcfg_args_t *netcfg_args)
{
    memset(&g_bt_netcfg_handle, 0, sizeof(g_bt_netcfg_handle));
    return netcfg_register(NETCFG_TUYA_BLE, ble_netcfg_start, ble_netcfg_stop);
}
```

#### BLE配网数据处理

```c
static void __handle_net_cfg(void *data, void *user_data)
{
    // 1. 解析JSON配网数据
    cJSON *json = cJSON_Parse(data);
    
    // 2. 提取配网信息
    char *ssid = cJSON_GetObjectItem(json, "ssid")->valuestring;
    char *token = cJSON_GetObjectItem(json, "token")->valuestring;
    char *passwd = cJSON_GetObjectItem(json, "pwd")->valuestring;
    
    // 3. 保存配网信息
    strncpy((char *)g_bt_netcfg_handle.netcfg_info.ssid, ssid, WIFI_SSID_LEN + 1);
    g_bt_netcfg_handle.netcfg_info.s_len = strlen(ssid);
    
    // 4. 触发配网完成回调
    g_bt_netcfg_handle.netcfg_finish_cb(NETCFG_TUYA_BLE, 
                                        &g_bt_netcfg_handle.netcfg_info);
}
```

### 3. 配网完成处理

```c
OPERATE_RET __netconn_wifi_netcfg_finish(int type, netcfg_info_t *info)
{
    netmgr_conn_wifi_t *netmgr_wifi = &s_netmgr_wifi;
    
    // 1. 保存WiFi配置
    memcpy(netmgr_wifi->conn.wifi_conn_info.ssid, info->ssid, info->s_len);
    memcpy(netmgr_wifi->conn.wifi_conn_info.pswd, info->passwd, info->p_len);
    __netconn_wifi_info_set(&netmgr_wifi->conn.wifi_conn_info);
    
    // 2. 停止所有配网
    netcfg_stop(NETCFG_STOP_ALL_CFG_MODULE);
    
    // 3. 解析Token信息
    tuya_binding_info_t binding;
    memset(&binding, 0, sizeof(tuya_binding_info_t));
    memcpy(binding.region, info->token, REGION_LEN);
    memcpy(binding.token, info->token + REGION_LEN, TOKEN_LEN);
    memcpy(binding.regist_key, info->token + REGION_LEN + TOKEN_LEN, REGIST_KEY_LEN);
    
    // 4. 发布激活事件
    tal_event_publish(EVENT_LINK_ACTIVATE, &binding);
    
    // 5. 连接WiFi
    __netconn_wifi_connect();
    
    return OPRT_OK;
}
```

## 完整API参考

### 1. 核心初始化API

#### tuya_iot_init()
```c
/**
 * @brief 初始化Tuya IoT客户端
 * 
 * @param client 客户端句柄指针
 * @param config 配置参数
 * @return OPRT_OK 成功，其他值表示错误
 */
int tuya_iot_init(tuya_iot_client_t *client, const tuya_iot_config_t *config);
```

#### tuya_iot_start()
```c
/**
 * @brief 启动Tuya IoT服务
 * 
 * @param client 客户端句柄
 * @return OPRT_OK 成功，其他值表示错误
 */
int tuya_iot_start(tuya_iot_client_t *client);
```

#### tuya_iot_yield()
```c
/**
 * @brief IoT客户端主循环处理
 * 
 * @param client 客户端句柄
 * @return OPRT_OK 成功，其他值表示错误
 */
int tuya_iot_yield(tuya_iot_client_t *client);
```

### 2. 激活相关API

#### atop_service_activate_request()
```c
/**
 * @brief 发送设备激活请求
 * 
 * @param request 激活请求参数
 * @param response 激活响应数据
 * @return OPRT_OK 成功，其他值表示错误
 */
int atop_service_activate_request(const tuya_activite_request_t *request, 
                                  atop_base_response_t *response);
```

#### atop_base_request()
```c
/**
 * @brief 发送ATOP基础HTTP请求
 * 
 * @param request 请求参数
 * @param response 响应数据
 * @return OPRT_OK 成功，其他值表示错误
 */
int atop_base_request(const atop_base_request_t *request, 
                      atop_base_response_t *response);
```

### 3. 网络管理API

#### netmgr_init()
```c
/**
 * @brief 初始化网络管理器
 * 
 * @param type 网络类型（WiFi/有线/自动）
 * @return OPRT_OK 成功，其他值表示错误
 */
OPERATE_RET netmgr_init(netmgr_type_e type);
```

#### netmgr_conn_set()
```c
/**
 * @brief 设置网络连接参数
 * 
 * @param type 网络类型
 * @param cmd 配置命令
 * @param param 参数指针
 * @return OPRT_OK 成功，其他值表示错误
 */
OPERATE_RET netmgr_conn_set(netmgr_type_e type, 
                             netmgr_conn_config_type_e cmd, 
                             void *param);
```

### 4. 配网API

#### netcfg_start()
```c
/**
 * @brief 启动网络配置
 * 
 * @param type 配网类型（AP/BLE等）
 * @param netcfg_finish_cb 配网完成回调
 * @param args 额外参数
 * @return OPRT_OK 成功，其他值表示错误
 */
int netcfg_start(int type, netcfg_finish_cb_t netcfg_finish_cb, void *args);
```

#### netcfg_stop()
```c
/**
 * @brief 停止网络配置
 * 
 * @param type 配网类型，0表示停止所有
 * @return OPRT_OK 成功，其他值表示错误
 */
int netcfg_stop(int type);
```

### 5. WiFi管理API

#### tal_wifi_init()
```c
/**
 * @brief 初始化WiFi模块
 * 
 * @param cb WiFi事件回调函数
 * @return OPRT_OK 成功，其他值表示错误
 */
OPERATE_RET tal_wifi_init(WF_EVENT_FUNC_CB cb);
```

#### tal_wifi_station_connect()
```c
/**
 * @brief 连接WiFi网络
 * 
 * @param ssid WiFi名称
 * @param password WiFi密码
 * @return OPRT_OK 成功，其他值表示错误
 */
OPERATE_RET tal_wifi_station_connect(const int8_t *ssid, const int8_t *password);
```

## 示例代码实现

### 1. 完整设备初始化示例

```c
#include "tuya_iot.h"
#include "netmgr.h"
#include "netconn_wifi.h"

// 全局变量
static tuya_iot_client_t g_client;
static tuya_iot_license_t g_license;

// 事件处理回调
void device_event_handler(tuya_iot_client_t *client, tuya_event_msg_t *event)
{
    switch (event->id) {
    case TUYA_EVENT_BIND_START:
        printf("设备开始绑定流程\n");
        break;
        
    case TUYA_EVENT_DIRECT_MQTT_CONNECTED:
        printf("设备直连MQTT成功，等待APP扫码绑定\n");
        // 生成二维码供APP扫描
        char qr_url[256];
        sprintf(qr_url, "https://smartapp.tuya.com/s/p?p=%s&uuid=%s&v=2.0", 
                PRODUCT_KEY, g_license.uuid);
        printf("二维码URL: %s\n", qr_url);
        break;
        
    case TUYA_EVENT_BIND_TOKEN_ON:
        printf("收到绑定Token: %s\n", event->value.asString);
        break;
        
    case TUYA_EVENT_ACTIVATE_SUCCESSED:
        printf("设备激活成功\n");
        break;
        
    case TUYA_EVENT_MQTT_CONNECTED:
        printf("设备MQTT连接成功，设备已上线\n");
        break;
        
    case TUYA_EVENT_TIMESTAMP_SYNC:
        printf("时间同步: %d\n", event->value.asInteger);
        tal_time_set_posix(event->value.asInteger, 1);
        break;
        
    default:
        break;
    }
}

// 网络状态检查
bool network_status_check(void)
{
    netmgr_status_e status = NETMGR_LINK_DOWN;
    netmgr_conn_get(NETCONN_AUTO, NETCONN_CMD_STATUS, &status);
    return (status == NETMGR_LINK_UP);
}

int main(void)
{
    int ret = OPRT_OK;
    
    // 1. 系统初始化
    cJSON_InitHooks(&(cJSON_Hooks){.malloc_fn = tal_malloc, .free_fn = tal_free});
    tal_log_init(TAL_LOG_LEVEL_DEBUG, 1024, (TAL_LOG_OUTPUT_CB)printf);
    tal_kv_init(&(tal_kv_cfg_t){
        .seed = "seed_string_16_chars",
        .key = "key_string_16_chars_",
    });
    tal_sw_timer_init();
    tal_workq_init();
    
    // 2. 读取设备授权信息
    if (OPRT_OK != tuya_authorize_read(&g_license)) {
        g_license.uuid = "your_device_uuid";
        g_license.authkey = "your_device_authkey";
        printf("警告：请替换真实的UUID和AuthKey\n");
    }
    
    // 3. 初始化IoT客户端
    ret = tuya_iot_init(&g_client, &(const tuya_iot_config_t){
        .software_ver = "1.0.0",
        .productkey = "your_product_key",
        .uuid = g_license.uuid,
        .authkey = g_license.authkey,
        .event_handler = device_event_handler,
        .network_check = network_status_check,
    });
    
    if (ret != OPRT_OK) {
        printf("IoT客户端初始化失败: %d\n", ret);
        return -1;
    }
    
    // 4. 初始化网络管理器
    netmgr_type_e net_type = 0;
#ifdef ENABLE_WIFI
    net_type |= NETCONN_WIFI;
#endif
#ifdef ENABLE_WIRED  
    net_type |= NETCONN_WIRED;
#endif
    
    ret = netmgr_init(net_type);
    if (ret != OPRT_OK) {
        printf("网络管理器初始化失败: %d\n", ret);
        return -1;
    }
    
    // 5. 设置配网模式
    if (!tuya_iot_activated(&g_client)) {
        netcfg_args_t netcfg_args = {
            .type = NETCFG_TUYA_WIFI_AP | NETCFG_TUYA_BLE,
            .uuid = (char *)g_license.uuid,
            .pincode = NULL
        };
        
        ret = netmgr_conn_set(NETCONN_WIFI, NETCONN_CMD_NETCFG, &netcfg_args);
        if (ret != OPRT_OK) {
            printf("配网设置失败: %d\n", ret);
            return -1;
        }
    }
    
    // 6. 启动IoT服务
    ret = tuya_iot_start(&g_client);
    if (ret != OPRT_OK) {
        printf("IoT服务启动失败: %d\n", ret);
        return -1;
    }
    
    printf("设备初始化完成，开始主循环\n");
    
    // 7. 主循环
    while (1) {
        tuya_iot_yield(&g_client);
        tal_system_sleep(10);
    }
    
    return 0;
}
```

### 2. 手动WiFi配置示例

```c
// 手动设置WiFi连接（适用于已知WiFi信息的场景）
void manual_wifi_connect(const char *ssid, const char *password)
{
    netconn_wifi_info_t wifi_info = {0};
    
    // 设置WiFi信息
    strncpy(wifi_info.ssid, ssid, WIFI_SSID_LEN);
    strncpy(wifi_info.pswd, password, WIFI_PASSWD_LEN);
    
    // 连接WiFi
    int ret = netmgr_conn_set(NETCONN_WIFI, NETCONN_CMD_SSID_PSWD, &wifi_info);
    if (ret == OPRT_OK) {
        printf("开始连接WiFi: %s\n", ssid);
    } else {
        printf("WiFi连接设置失败: %d\n", ret);
    }
}
```

### 3. 自定义配网完成处理

```c
// 配网完成回调处理
int custom_netcfg_finish_callback(int type, netcfg_info_t *info)
{
    printf("配网完成，类型: %d\n", type);
    printf("SSID: %.*s\n", info->s_len, info->ssid);
    printf("密码: %.*s\n", info->p_len, info->passwd);
    printf("Token: %.*s\n", info->t_len, info->token);
    
    // 这里可以添加自定义的配网完成处理逻辑
    // 比如LED指示灯变化、蜂鸣器提示等
    
    return OPRT_OK;
}
```

## 最佳实践

### 1. 错误处理

```c
// 错误代码处理
const char *get_error_string(int error_code)
{
    switch (error_code) {
    case OPRT_OK:
        return "成功";
    case OPRT_INVALID_PARM:
        return "参数无效";
    case OPRT_MALLOC_FAILED:
        return "内存分配失败";
    case OPRT_RESOURCE_NOT_READY:
        return "资源未准备就绪";
    case OPRT_COM_ERROR:
        return "通信错误";
    default:
        return "未知错误";
    }
}

// 带错误处理的API调用
#define SAFE_CALL(func) do { \
    int ret = (func); \
    if (ret != OPRT_OK) { \
        printf("函数 %s 执行失败: %s (%d)\n", #func, get_error_string(ret), ret); \
        return ret; \
    } \
} while(0)
```

### 2. 状态管理

```c
// 设备状态枚举
typedef enum {
    DEVICE_STATE_INIT,
    DEVICE_STATE_NETCFG,
    DEVICE_STATE_CONNECTING,
    DEVICE_STATE_ACTIVATING, 
    DEVICE_STATE_ONLINE,
    DEVICE_STATE_OFFLINE,
    DEVICE_STATE_ERROR
} device_state_e;

static device_state_e g_device_state = DEVICE_STATE_INIT;

// 状态变更处理
void device_state_change(device_state_e new_state)
{
    if (g_device_state != new_state) {
        printf("设备状态变更: %d -> %d\n", g_device_state, new_state);
        g_device_state = new_state;
        
        // 根据状态执行相应操作
        switch (new_state) {
        case DEVICE_STATE_NETCFG:
            // 启动配网指示
            break;
        case DEVICE_STATE_ONLINE:
            // 设备上线指示
            break;
        case DEVICE_STATE_ERROR:
            // 错误指示
            break;
        default:
            break;
        }
    }
}
```

### 3. 重连机制

```c
// 网络重连处理
void handle_network_disconnect(void)
{
    static int reconnect_count = 0;
    const int max_reconnect = 5;
    const int reconnect_interval[] = {1, 3, 5, 10, 30}; // 秒
    
    if (reconnect_count < max_reconnect) {
        int delay = reconnect_interval[reconnect_count];
        printf("网络断开，%d秒后重连（第%d次）\n", delay, reconnect_count + 1);
        
        // 延时后重连
        tal_sw_timer_start(reconnect_timer, delay * 1000, TAL_TIMER_ONCE);
        reconnect_count++;
    } else {
        printf("重连次数超限，进入错误状态\n");
        device_state_change(DEVICE_STATE_ERROR);
    }
}

// 网络连接成功处理
void handle_network_connected(void)
{
    printf("网络连接成功\n");
    reconnect_count = 0; // 重置重连计数
    device_state_change(DEVICE_STATE_ONLINE);
}
```

### 4. 日志管理

```c
// 日志级别控制
void setup_logging(void)
{
#ifdef DEBUG
    tal_log_init(TAL_LOG_LEVEL_DEBUG, 2048, (TAL_LOG_OUTPUT_CB)printf);
#else
    tal_log_init(TAL_LOG_LEVEL_INFO, 1024, (TAL_LOG_OUTPUT_CB)printf);
#endif
}

// 关键事件日志
void log_critical_event(const char *event, int result)
{
    printf("[CRITICAL] %s: %s (%d)\n", event, 
           (result == OPRT_OK) ? "成功" : "失败", result);
}
```

### 5. 资源管理

```c
// 内存监控
void memory_monitor(void)
{
    static uint32_t last_free_heap = 0;
    uint32_t current_free_heap = tal_system_get_free_heap_size();
    
    if (last_free_heap > 0) {
        int32_t diff = current_free_heap - last_free_heap;
        if (abs(diff) > 1024) { // 变化超过1KB时记录
            printf("内存变化: %s %d 字节，当前可用: %u 字节\n",
                   (diff > 0) ? "释放" : "分配", abs(diff), current_free_heap);
        }
    }
    
    last_free_heap = current_free_heap;
    
    // 内存不足告警
    if (current_free_heap < 4096) {
        printf("警告：可用内存不足 %u 字节\n", current_free_heap);
    }
}
```

## 总结

TuyaOpen设备激活绑定流程是一个完整的端到端过程，涉及设备初始化、网络配网、HTTP激活和MQTT连接等多个步骤。通过本指南提供的API参考和示例代码，开发者可以：

1. **理解完整流程**：掌握从设备启动到云端连接的完整过程
2. **使用核心API**：正确调用激活、配网和网络管理相关接口
3. **实现可靠连接**：通过错误处理和重连机制确保稳定性
4. **优化用户体验**：提供清晰的状态指示和操作反馈

在实际开发中，建议根据具体的硬件平台和应用场景，选择合适的配网方式和网络管理策略，确保设备能够稳定可靠地接入涂鸦云平台。 